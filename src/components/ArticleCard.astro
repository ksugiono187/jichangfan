---
import type { CollectionEntry } from 'astro:content';
interface Props {
  post: CollectionEntry<'blog'>;
}
const { post } = Astro.props;
---
<a href={`/blog/${post.id}`} class="group flex flex-col bg-zinc-900 rounded-2xl border border-zinc-800 shadow-sm hover:shadow-xl transition-all duration-300 overflow-hidden">
  <div class="p-6 flex-grow">
    <div class="flex items-center gap-2 mb-4">
      <span class="text-xs font-semibold text-amber-500 bg-amber-900/20 px-2.5 py-1 rounded-full">{post.data.category}</span>
      <time class="text-xs text-zinc-400">{post.data.pubDate.toLocaleDateString('zh-CN')}</time>
    </div>
    <h3 class="text-xl font-bold text-zinc-50 mb-3 group-hover:text-amber-500 transition-colors line-clamp-2">{post.data.title}</h3>
    <p class="text-zinc-400 line-clamp-3 text-sm leading-relaxed mb-4">{post.data.description}</p>
  </div>
  <div class="px-6 py-4 border-t border-gray-50 mt-auto flex justify-between items-center">
    <span class="text-sm font-medium text-zinc-50">{post.data.author}</span>
    <span class="text-sm font-medium text-amber-500 flex items-center gap-1 group-hover:translate-x-1 transition-transform">阅读全文 <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg></span>
  </div>
</a>
